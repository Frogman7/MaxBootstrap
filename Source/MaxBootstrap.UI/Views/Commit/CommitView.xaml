<UserControl x:Class="MaxBootstrap.UI.Views.Commit.CommitView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MaxBootstrap.UI.Views.Commit"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance local:CommitViewmodel}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid>
            <TextBlock Text="{Binding CommitMessage}" FontSize="28" TextAlignment="Center" TextWrapping="Wrap" Margin="10,0,10,0"/>
        </Grid>
    </Grid>
</UserControl>
